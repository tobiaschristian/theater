<?xml version="1.0"?>
<ruleset name="symfony_mailer">
  <description>Default PHP CodeSniffer configuration for Mailer Plus.</description>
  <file>.</file>

  <rule ref="Drupal">
    <!-- The following rules are intentionally disabled. -->
    <exclude name="Drupal.Arrays.Array.LongLineDeclaration"/>
    <exclude name="DrupalPractice.Objects.GlobalDrupal.GlobalDrupal"/>
    <exclude name="DrupalPractice.Objects.GlobalClass.GlobalClass"/>
  </rule>

  <rule ref="DrupalPractice"/>

</ruleset>
